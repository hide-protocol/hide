target/
node_modules/
.copilot-tmp/
*.test-secret
*.hide
!conformance/vectors/*.hide

# The published test vectors are fixtures, not credentials: the "secret" is the
# fixed all-0x55 seed from generate_vectors.rs and protects nothing.
!conformance/vectors/*.test-secret
.pnpm-cache/

# Desktop app build output. `gen/schemas` is regenerated by tauri-build.
apps/hide-desktop/dist/
apps/hide-desktop/src-tauri/gen/
