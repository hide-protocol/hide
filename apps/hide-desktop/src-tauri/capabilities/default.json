{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "File pickers only. The application has no network, shell or filesystem-wide access; every read and write goes through a Rust command on a path the user picked.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message"
  ]
}
